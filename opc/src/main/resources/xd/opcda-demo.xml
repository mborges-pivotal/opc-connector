<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:int="http://www.springframework.org/schema/integration"
	xmlns:task="http://www.springframework.org/schema/task"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/integration
		http://www.springframework.org/schema/integration/spring-integration.xsd http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task.xsd">

	<int:inbound-channel-adapter ref="opc"
		method="generate" channel="output">
		<int:poller task-executor="executor" fixed-delay="${fixedDelay}"
			max-messages-per-poll="${maxMsgPoll}" />
	</int:inbound-channel-adapter>

	<bean id="opc" class="com.gopivotal.tola.opc.xd.OpcPollingSource">
		<property name="host" value="${host}" />
		<property name="domain" value="${domain}" />
		<property name="username" value="${username}" />
		<property name="password" value="${password}" />
		<property name="progid" value="${progid}" />
		<property name="tags" value="${tags}" />
	</bean>

	<task:executor id="executor" pool-size="${poolSize}" />

	<int:channel id="output" />
</beans>